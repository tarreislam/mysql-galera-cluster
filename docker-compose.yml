
services:
  mariadb:
    build: .
    container_name: mariadb
    environment:
      MYSQL_ROLE: nothing
    network_mode: "host"
    volumes:
      - ./mariadb_data:/var/lib/mysql
  mariadb-new-cluster:
    build: .
    container_name: mariadb
    environment:
      MYSQL_ROLE: wsrep-new-cluster
    network_mode: "host"
    volumes:
      - ./mariadb_data:/var/lib/mysql