1. cp .env.example
2. use "env-calculator.htmll" to calculate values
3. edit .env with calculated values and other config for cluster
4. `docker-compose up -d mariadb`


# Crash recovery
if the role is master. It will try to start a new cluster all the time
